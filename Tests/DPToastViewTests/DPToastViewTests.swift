import XCTest
@testable import DPToastView

final class DPToastViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(DPToastView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
